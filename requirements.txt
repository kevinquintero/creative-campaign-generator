flask>=3.0.0
pillow>=10.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
openai>=1.0.0       # optional — only needed when IMAGE_PROVIDER=openai
google-genai>=1.0.0 # optional — only needed when IMAGE_PROVIDER=gemini
